name: To GitCode ☞ folder incremental ✔

on:
  # UTC 时间22:05触发，北京时间次日6:05
  schedule:
    - cron: '5 22 * * *'
  workflow_dispatch:
    inputs:
      source-folder:
        description: '源文件夹'
        required: false
        default: 'output'
      target-repo:
        description: 'GitCode 仓库名'
        required: false
        default: 'update'
      target-subdir:
        description: 'GitCode 子目录'
        required: false
        default: 'output'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      # 1. 检出源仓库（GitHub 源仓库）
      - name: Checkout Source
        uses: actions/checkout@v4
        with:
          path: src

      # 2. 克隆目标仓库（修正为正确的 GitCode 地址）
      - name: Clone Target
        run: |
          #GitCode 仓库地址（用户名xiaoran79，仓库名update）
          GITCODE_USER="xiaoran79"
          GITCODE_REPO="${{ inputs.target-repo || 'update' }}"
          git clone --depth=1 \
            "https://${GITCODE_USER}:${{ secrets.GITCODE_ACCESS_TOKEN }}@gitcode.com/${GITCODE_USER}/${GITCODE_REPO}.git" \
            target

      # 3. 计算源文件夹哈希（校验文件一致性）
      - name: Hash Source
        id: hash_src
        run: |
          cd src
          SRC_HASH=$(find "${{ inputs.source-folder || 'output' }}" -type f -exec sha1sum {} + | sort | sha1sum | awk '{print $1}')
          echo "SRC_HASH=$SRC_HASH" >> $GITHUB_ENV

      # 4. 计算目标文件夹哈希（不存在则设默认值）
      - name: Hash Target
        id: hash_tgt
        run: |
          cd target
          TGT_HASH=$(find "${{ inputs.target-subdir || 'output' }}" -type f -exec sha1sum {} + 2>/dev/null | sort | sha1sum | awk '{print $1}')
          if [ -z "$TGT_HASH" ]; then
            TGT_HASH="0000000000000000000000000000000000000000"
          fi
          echo "TGT_HASH=$TGT_HASH" >> $GITHUB_ENV

      # 5. 比较哈希值，判断是否需要同步
      - name: Compare Hashes
        run: |
          echo "源文件夹哈希: ${{ env.SRC_HASH }}"
          echo "目标文件夹哈希: ${{ env.TGT_HASH }}"
          if [ "${{ env.SRC_HASH }}" = "${{ env.TGT_HASH }}" ]; then
            echo "✅ 内容一致，跳过同步"
            echo "SYNC_NEEDED=false" >> $GITHUB_ENV
          else
            echo "🔄 内容不一致，执行增量同步"
            echo "SYNC_NEEDED=true" >> $GITHUB_ENV
          fi

      # 6. 增量同步（保留差异文件，删除目标端多余文件）
      - name: Incremental Sync
        if: env.SYNC_NEEDED == 'true'
        run: |
          rsync -av --delete "src/${{ inputs.source-folder || 'output' }}/" "target/${{ inputs.target-subdir || 'output' }}/"

      # 7. 检查目标仓库是否有实际文件变化
      - name: Check Target Changes
        if: env.SYNC_NEEDED == 'true'
        id: check_changes
        run: |
          cd target
          git status --porcelain
          if [ -n "$(git status --porcelain)" ]; then
            echo "CHANGES_DETECTED=true" >> $GITHUB_ENV
          else
            echo "CHANGES_DETECTED=false" >> $GITHUB_ENV
          fi

      # 8. 提交并推送更新（仅当有变化时）
      - name: Commit and Push
        if: env.SYNC_NEEDED == 'true' && env.CHANGES_DETECTED == 'true'
        run: |
          cd target
          git config user.name "Auto Sync Bot"
          git config user.email "auto-sync@noreply.github.com"
          git add -A
          git commit -m "✅$(TZ='Asia/Shanghai' date +'%Y-%m-%d %H:%M:%S')"
          git push origin HEAD

      # 9. 输出同步结果报告
      - name: Sync Report
        run: |
          if [ "$SYNC_NEEDED" = "true" ]; then
            if [ "$CHANGES_DETECTED" = "true" ]; then
              echo "✅ 已完成 GitCode 增量同步并推送更新"
            else
              echo "ℹ️ 增量同步未检测到实际文件变化（可能为哈希冲突）"
            fi
          else
            echo "ℹ️ 无需同步：GitHub 源与 GitCode 目标内容一致"
          fi
